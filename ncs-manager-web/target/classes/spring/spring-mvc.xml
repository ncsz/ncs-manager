<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">
	<!-- 为什么不能配全局扫描? 访问404-->
	<!-- springmvc和spring父子容器关系 -->
	<!-- spring是容器，放好多对象   
	springmvc也是个容器，在spring内部，springmvc里面存的就是controller
	spring容器中存的是service和dao，平时dao注入service不会有问题
	现在service注入controller也可以注入，是因为子容器可以访问父容器中的对象
	但父容器不能访问子容器对象，service里面不能访问controller
	如果配了全局的扫描，所有对象都会放在spring容器中，springmvc中没有对象，所以访问会404 -->
	<context:component-scan base-package="com.ncs.controller" />
	<mvc:annotation-driven />
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/" />
		<property name="suffix" value=".jsp" />
	</bean>
	<!-- 添加静态资源 -->
	<mvc:resources mapping="/img/**" location="/WEB-INF/img/" />
    <mvc:resources mapping="/css/**" location="/WEB-INF/css/" />
    <mvc:resources mapping="/js/**" location="/WEB-INF/js/" />
</beans>